services:
  bot:
    build: .
    container_name: devcoder-privat-bot
    restart: unless-stopped
    # Environment variables are set in Portainer stack UI
    ports:
      - "6062:6062"
    volumes:
      - ./logs:/app/logs
      - ./transcripts:/app/transcripts
